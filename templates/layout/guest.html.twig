{#
 # Guest shell (no sidebar), used by the login page.
 # Port of the Laravel core `layouts/master-without-nav`.
 #
 # Blocks: title, stylesheets, javascripts, body_attributes, body, content.
 #}
{% extends '@GingermindsCore/layout/_document.html.twig' %}

{% block body %}
    <div class="account-pages py-5">
        <div class="container">
            {% block content %}{% endblock %}
        </div>
    </div>
{% endblock %}
